# Project Goals for Today:
- Finishing Building the Autonomous 
- Set up the Nvidia Operating System on the Jetson Nano
- Implement SSH on the OS to be able to remotely do stuff on the Jetson Nano OS
- Do Research on Task \[B] Camera Fusion

### Plan for Task 
### Plan for Task 2: Camera Fusion for Perception
##### With Gemini:
"Camera Fusion" in this context usually refers to one of two things: **Temporal Fusion** (combining frames over time)

**Key Concepts to Research:**
- **Object Detection:** Use **YOLOv8** or **SSD-Mobilenet**. These are optimized for the Jetson Nano's GPU using **TensorRT** to ensure you get high frames per second (FPS).
- **Lane Detection:** This usually involves **Color Masking** (HLS/HSV color space) and the **Hough Transform** to identify lines.
- **The "Fusion" Logic:** You aren't just running two models; you are creating a "World Model."
    - _Example:_ If the Lane Detection says "Turn Left" but Object Detection says "Obstacle Ahead," your perception layer must "fuse" these into a single command: "Stop."

1. Capture video from _your_ vehicle's specific camera angle.
2. Optimization with TensorRT
	1. The Jetson Nano is powerful but has limited RAM (4GB or 2GB).
	2. **Practice:** Research how to convert a standard PyTorch/TensorFlow model into a **TensorRT engine**. This can increase your speed from 5 FPS to 25+ FPS, which is critical for a moving vehicle.
3. Failure Mode Analysis (Critical Thinking)
	1. Your grading rubric mentions "critical thinking."
	2. **Research:** What happens when the camera gets blurry? What happens if a shadow looks like a lane line?
	3. **Plan:** Implement a "Confidence Score" threshold. If the model is less than 50% sure, the car should safely stop.